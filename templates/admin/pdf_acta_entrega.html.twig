<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: 'Helvetica', sans-serif; font-size: 12px; color: #333; }
        .header { text-align: center; margin-bottom: 30px; border-bottom: 2px solid #444; padding-bottom: 10px; }
        .header h1 { margin: 0; font-size: 18px; text-transform: uppercase; }
        .header h2 { margin: 5px 0; font-size: 14px; color: #666; }

        .title { text-align: center; background-color: #f2f2f2; padding: 10px; margin-bottom: 20px; font-weight: bold; font-size: 14px; }

        table { width: 100%; border-collapse: collapse; margin-bottom: 20px; }
        th, td { border: 1px solid #ccc; padding: 8px; text-align: left; }
        th { background-color: #f9f9f9; width: 30%; font-weight: bold; }

        .signature-section { margin-top: 40px; }
        .signature-grid { width: 100%; }
        .signature-box { border: 1px dashed #999; padding: 15px; text-align: center; height: 100px; vertical-align: top; }
        .signature-label { font-weight: bold; font-size: 10px; margin-bottom: 5px; display: block; text-transform: uppercase; color: #555; }
        .signature-data { font-size: 11px; color: #000; margin-top: 10px; }
        .timestamp { font-size: 9px; color: #777; font-style: italic; }

        .footer { position: fixed; bottom: 0; width: 100%; text-align: center; font-size: 9px; color: #999; border-top: 1px solid #eee; padding-top: 5px; }
    </style>
</head>
<body>

    <div class="header">
        <h1>Nombre de la Institución</h1>
        <h2>Sistema Nacional de Gestión de Activos Fijos</h2>
    </div>

    <div class="title">ACTA DE ENTREGA Y TRASLADO DE ACTIVO #{{ t.id }}</div>

    <table>
        <tr>
            <th>Fecha de Emisión</th>
            <td>{{ fecha|date('d/m/Y H:i') }}</td>
        </tr>
        <tr>
            <th>Tipo de Operación</th>
            <td>{{ t.type }}</td>
        </tr>
        <tr>
            <th>Estado del Proceso</th>
            <td style="color: green; font-weight: bold;">{{ t.status }}</td>
        </tr>
    </table>

    <div style="font-weight: bold; margin-bottom: 10px;">DATOS DEL ACTIVO</div>
    <table>
        <tr>
            <th>Descripción/Nombre</th>
            <td>{{ t.asset.name }}</td>
        </tr>
        <tr>
            <th>Marca / Serie</th>
            <td>{{ t.asset.brand }} / {{ t.asset.serial }}</td>
        </tr>
        <tr>
            <th>Código de Inventario</th>
            <td>{{ t.asset.assetNumber }}</td>
        </tr>
        <tr>
            <th>Categoría</th>
            <td>{{ t.asset.category }}</td>
        </tr>
    </table>

    <div style="font-weight: bold; margin-bottom: 10px;">DETALLES DEL MOVIMIENTO</div>
    <table>
        <tr>
            <th>Departamento Origen</th>
            <td>{{ t.originDepartment ? t.originDepartment.name : 'N/A' }}</td>
        </tr>
        <tr>
            <th>Departamento Destino</th>
            <td>{{ t.destinationDepartment ? t.destinationDepartment.name : 'N/A' }}</td>
        </tr>
        <tr>
            <th>Funcionario Responsable</th>
            <td>{{ t.assignedTo }}</td>
        </tr>
        <tr>
            <th>Observaciones</th>
            <td>{{ t.observations|default('Sin observaciones adicionales.') }}</td>
        </tr>
    </table>

    <div class="signature-section">
        <div style="font-weight: bold; margin-bottom: 10px; text-align: center;">REGISTRO DE FIRMAS DIGITALES Y VALIDACIONES</div>
        <table class="signature-grid">
            <tr>
                <td class="signature-box">
                    <span class="signature-label">Solicitante (Origen)</span>
                    <div class="signature-data">{{ t.user.email }}</div>
                    <div class="timestamp">Generado: {{ t.createdAt|date('d/m/Y H:i') }}</div>
                </td>
                <td class="signature-box">
                    <span class="signature-label">Recibido por (Destino)</span>
                    <div class="signature-data">{{ t.receivedBy ? t.receivedBy.email : 'PENDIENTE' }}</div>
                    {% if t.receivedAt %}
                        <div class="timestamp">Firmado: {{ t.receivedAt|date('d/m/Y H:i') }}</div>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <td class="signature-box">
                    <span class="signature-label">Visto Bueno Tecnología</span>
                    <div class="signature-data">{{ t.techApprovedBy ? t.techApprovedBy.email : 'NO REQUERIDO / PENDIENTE' }}</div>
                    {% if t.techApprovedAt %}
                        <div class="timestamp">Validado: {{ t.techApprovedAt|date('d/m/Y H:i') }}</div>
                    {% endif %}
                </td>
                <td class="signature-box">
                    <span class="signature-label">Autorización Contabilidad</span>
                    <div class="signature-data">{{ t.accountingApprovedBy ? t.accountingApprovedBy.email : 'PENDIENTE' }}</div>
                    {% if t.accountingApprovedAt %}
                        <div class="timestamp">Validado: {{ t.accountingApprovedAt|date('d/m/Y H:i') }}</div>
                    {% endif %}
                </td>
            </tr>
        </table>
    </div>

    <div class="footer">
        Este documento es una representación digital de una transacción autorizada en el Sistema de Inventario.<br>
        Fecha de impresión: {{ "now"|date('d/m/Y H:i:s') }}
    </div>

</body>
</html>
